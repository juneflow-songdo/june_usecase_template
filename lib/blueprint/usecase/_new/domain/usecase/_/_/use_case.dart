import '../../../../../../../main.dart';

class _NewUseCase {
  Future<dynamic> run(String query) async {
    // await repo.function1(query);
  }

// final SomeRepoInterface repo;
// _NewUseCase(this.repo);
}

class NewUseCase {
  // static final _NewUseCase useCase = _NewUseCase(SomeRepo());
}
